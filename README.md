# portfolio

A simple starter website for my personal portfolio.

## Run locally

Because this is a static site, you can open `index.html` directly in a browser or run:

```bash
python -m http.server
```

Then open `http://localhost:8000`.
